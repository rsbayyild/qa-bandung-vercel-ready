# Required: server-side only. Add this in Vercel > Project > Settings > Environment Variables.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# Optional. Defaults to gemini-3.5-flash.
GEMINI_MODEL="gemini-3.5-flash"
