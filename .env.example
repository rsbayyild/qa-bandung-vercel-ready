# Required: server-side only. Add in Vercel > Project > Settings > Environment Variables.
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# Optional. Balanced default for OCR + multimodal summarization.
OPENAI_MODEL="gpt-5.6-terra"
