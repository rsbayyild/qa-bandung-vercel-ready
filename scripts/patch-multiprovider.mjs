import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const appPath = path.join(root, "src", "App.tsx");
let source = fs.readFileSync(appPath, "utf8");
if (source.includes("AI_MODEL_OPTIONS")) { console.log("Multi-provider UI already patched."); process.exit(0); }
const patch = "--- src/App.tsx\n+++ src/App.tsx\n@@ -174,6 +174,31 @@\n   }\n ];\n \n+\n+type AiProvider = \"openai\" | \"glm\" | \"deepseek\";\n+\n+const AI_MODEL_OPTIONS: Record<AiProvider, Array<{ value: string; label: string; vision: boolean }>> = {\n+  openai: [\n+    { value: \"gpt-5.6-terra\", label: \"GPT-5.6 Terra\", vision: true },\n+    { value: \"gpt-5.6-luna\", label: \"GPT-5.6 Luna\", vision: true },\n+    { value: \"gpt-5.6-sol\", label: \"GPT-5.6 Sol\", vision: true },\n+  ],\n+  glm: [\n+    { value: \"glm-4.6v\", label: \"GLM-4.6V\", vision: true },\n+    { value: \"glm-4.6v-flash\", label: \"GLM-4.6V Flash\", vision: true },\n+  ],\n+  deepseek: [\n+    { value: \"deepseek-v4-pro\", label: \"DeepSeek V4 Pro · text\", vision: false },\n+    { value: \"deepseek-v4-flash\", label: \"DeepSeek V4 Flash · text\", vision: false },\n+  ],\n+};\n+\n+const DEFAULT_AI_MODEL: Record<AiProvider, string> = {\n+  openai: \"gpt-5.6-terra\",\n+  glm: \"glm-4.6v-flash\",\n+  deepseek: \"deepseek-v4-flash\",\n+};\n+\n export default function App() {\n   // ── STATES ───────────────────────────────────────────────────────────────\n   const [items, setItems] = useState<Item[]>([]);\n@@ -194,6 +219,11 @@\n   const [isLoading, setIsLoading] = useState(false);\n   const [errorNotice, setErrorNotice] = useState<string | null>(null);\n \n+  // Runtime AI provider/model selector. API keys remain server-side in Vercel.\n+  const [aiProvider, setAiProvider] = useState<AiProvider>(\"openai\");\n+  const [aiModel, setAiModel] = useState<string>(DEFAULT_AI_MODEL.openai);\n+\n+\n   // States for interactive real-time field progress compliance report\n   const [progressAnalysisText, setProgressAnalysisText] = useState<string | null>(null);\n   const [isAnalyzingProgress, setIsAnalyzingProgress] = useState<boolean>(false);\n@@ -224,6 +254,16 @@\n     }, 4500);\n   };\n \n+  const changeAiProvider = (provider: AiProvider) => {\n+    setAiProvider(provider);\n+    setAiModel(DEFAULT_AI_MODEL[provider]);\n+    if (provider === \"deepseek\") {\n+      showToast(\"DeepSeek saat ini text-only; SCAN/RINGKASAN visual memerlukan OpenAI atau GLM.\", \"info\");\n+    } else {\n+      showToast(`AI Engine: ${provider === \"openai\" ? \"OpenAI\" : \"GLM\"}`, \"info\");\n+    }\n+  };\n+\n   // WIB (UTC+7) clock tracker\n   const [now, setNow] = useState<Date>(new Date());\n \n@@ -315,7 +355,9 @@\n         headers: { \"Content-Type\": \"application/json\" },\n         body: JSON.stringify({\n           items: aiItems,\n-          messages: chatMessagesForApi\n+          messages: chatMessagesForApi,\n+          provider: aiProvider,\n+          model: aiModel\n         }),\n       });\n \n@@ -1407,7 +1449,7 @@\n       const res = await fetch(\"/api/summarize\", {\n         method: \"POST\",\n         headers: { \"Content-Type\": \"application/json\" },\n-        body: JSON.stringify({ items: aiItems }),\n+        body: JSON.stringify({ items: aiItems, provider: aiProvider, model: aiModel }),\n       });\n \n       if (!res.ok) {\n@@ -1488,7 +1530,7 @@\n     playCling();\n   }, []);\n \n-  // Calls the server-side proxy `/api/analyze` ensuring process.env.GEMINI_API_KEY is secure\n+  // Calls the server-side AI proxy; provider API keys stay secure in Vercel\n   const requestFrictionlessAnalysis = useCallback(async (hitItem: Item, croppedB64: string, pinId: number) => {\n     try {\n       const response = await fetch(\"/api/analyze\", {\n@@ -1498,7 +1540,9 @@\n         },\n         body: JSON.stringify({\n           imageB64: croppedB64,\n-          type: hitItem.type\n+          type: hitItem.type,\n+          provider: aiProvider,\n+          model: aiModel\n         })\n       });\n \n@@ -1515,7 +1559,7 @@\n         currentPins.map(p => p.id === pinId ? { ...p, status: \"done\", result: analyzedJson } : p)\n       );\n     } catch (err: any) {\n-      console.error(\"Gemini server-side evaluation error:\", err);\n+      console.error(\"AI server-side evaluation error:\", err);\n       \n       const isQuotaError = err.message && (\n         err.message.toLowerCase().includes(\"quota\") || \n@@ -1526,7 +1570,7 @@\n \n       if (isQuotaError) {\n         setSimulationEnabled(true);\n-        setErrorNotice(\"Kuota harian Gemini API (Free Tier) telah habis. Mengaktifkan Mode Simulasi Offline agar Anda tetap lancar mendesain & menganalisis.\");\n+        setErrorNotice(\"Kuota AI provider sedang habis/terbatas. Mengaktifkan Mode Simulasi Offline agar Anda tetap dapat melanjutkan workflow.\");\n         applySimulatedResult(pinId, hitItem.type);\n         return;\n       }\n@@ -1942,12 +1986,14 @@\n           items: aiItems,\n           messages: [\n             { role: \"user\", content: promptText }\n-          ]\n+          ],\n+          provider: aiProvider,\n+          model: aiModel\n         })\n       });\n \n       if (!resp.ok) {\n-        throw new Error(\"Gagal mengaudit keselarasan dengan Gemini.\");\n+        throw new Error(\"Gagal mengaudit keselarasan dengan AI provider terpilih.\");\n       }\n \n       const data = await resp.json();\n@@ -2710,6 +2756,33 @@\n             <CheckCircle2 className=\"w-3 h-3 text-neutral-300/60\" />\n             <span>PROGRESS</span>\n           </button>\n+\n+          <div className=\"w-px h-5 bg-cod-800\" />\n+\n+          <div className=\"flex items-center gap-1 bg-cod-900/70 border border-cod-800 px-1 py-0.5 rounded-sm\" title=\"Pilih AI provider dan model. API key tetap tersimpan server-side di Vercel.\">\n+            <span className=\"text-[7px] font-black tracking-widest text-cod-500 px-1\">AI</span>\n+            <select\n+              value={aiProvider}\n+              onChange={(e) => changeAiProvider(e.target.value as AiProvider)}\n+              className=\"bg-cod-950 text-[8px] font-mono font-bold text-cod-200 border border-cod-800 px-1.5 py-1 outline-none cursor-pointer\"\n+              aria-label=\"AI Provider\"\n+            >\n+              <option value=\"openai\">OPENAI</option>\n+              <option value=\"glm\">GLM</option>\n+              <option value=\"deepseek\">DEEPSEEK</option>\n+            </select>\n+            <select\n+              value={aiModel}\n+              onChange={(e) => setAiModel(e.target.value)}\n+              className=\"bg-cod-950 text-[8px] font-mono font-bold text-cod-200 border border-cod-800 px-1.5 py-1 outline-none cursor-pointer max-w-[150px]\"\n+              aria-label=\"AI Model\"\n+              title={aiProvider === \"deepseek\" ? \"DeepSeek API resmi saat ini menerima input teks. Untuk OCR/gambar gunakan OpenAI atau GLM.\" : \"Model aktif untuk SCAN, Ringkasan, dan Chat\"}\n+            >\n+              {AI_MODEL_OPTIONS[aiProvider].map((option) => (\n+                <option key={option.value} value={option.value}>{option.label}</option>\n+              ))}\n+            </select>\n+          </div>\n \n           {items.length > 0 && (\n             <>\n";
const lines = source.split("\n");
let offset = 0;
const patchLines = patch.split("\n");
for (let i = 0; i < patchLines.length; i++) {
  const m = patchLines[i].match(/^@@ -(\d+)(?:,(\d+))? \+(\d+)(?:,(\d+))? @@/);
  if (!m) continue;
  const oldStart = Number(m[1]);
  const oldBlock = [];
  const newBlock = [];
  i++;
  while (i < patchLines.length && !patchLines[i].startsWith("@@ ")) {
    const line = patchLines[i];
    if (line.startsWith("--- ") || line.startsWith("+++ ")) { i++; continue; }
    if (line.startsWith(" ")) { oldBlock.push(line.slice(1)); newBlock.push(line.slice(1)); }
    else if (line.startsWith("-")) oldBlock.push(line.slice(1));
    else if (line.startsWith("+")) newBlock.push(line.slice(1));
    else if (line === "\\ No newline at end of file") {}
    else if (line === "") { /* trailing diff split */ }
    i++;
  }
  i--;
  const at = oldStart - 1 + offset;
  const actual = lines.slice(at, at + oldBlock.length);
  if (JSON.stringify(actual) !== JSON.stringify(oldBlock)) {
    throw new Error(`Multi-provider UI patch mismatch near original line ${oldStart}.`);
  }
  lines.splice(at, oldBlock.length, ...newBlock);
  offset += newBlock.length - oldBlock.length;
}
source = lines.join("\n");
fs.writeFileSync(appPath, source);
console.log("Patched multi-provider selector into src/App.tsx");
